[project]
name = "agents-matrix"
version = "0.1.0"
description = "Multi-agent platform — each agent wraps a CLI tool as a paid Agent-as-a-Service"
requires-python = ">=3.12"

[tool.uv]
prerelease = "allow"

[tool.uv.workspace]
members = ["framework", "agents/*"]
